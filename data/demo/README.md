# Demo data

The demo endpoint generates deterministic pose-feature sequences in memory.
They are synthetic and are labeled as demo data in the API and UI.

No real patient video or medical record is included. Uploaded user video is
written only to a temporary file, converted to derived landmarks/features, and
deleted immediately after analysis.

