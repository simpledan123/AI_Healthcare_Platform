# 기존 저장소에 반영하는 방법

이 ZIP은 기존 `.git` 이력을 포함하지 않는 깨끗한 교체본입니다.

1. 기존 로컬 저장소에서 새 브랜치를 만듭니다.
2. `.git` 폴더는 유지하고 나머지 기존 파일을 별도 백업합니다.
3. 이 ZIP 내부 파일을 저장소 루트에 복사합니다.
4. `.env.example`을 `.env`로 복사합니다.
5. README의 테스트와 빌드 명령을 실행합니다.
6. 변경 내용을 확인한 뒤 직접 커밋합니다.

기존 Prophet, LoRA, 커뮤니티, 레거시 프런트 코드는 완성본의 핵심 실행
경로에서 제외했습니다. 필요하면 기존 브랜치에서 `experiments/` 아래로
선별해 옮기되, 손목 동작 분석·검수 흐름과 섞지 않는 것을 권장합니다.

실제 사용자·전문가 영상은 포함하지 않았습니다. 전문가 기준 등록 화면을
통해 영상을 처리하면 원본은 임시 파일에서 삭제되고 파생 특징만 저장됩니다.

